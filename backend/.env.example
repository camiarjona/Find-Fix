# =============================================================================
# PLANTILLA DE VARIABLES DE ENTORNO
# =============================================================================
# INSTRUCCIONES PARA EL DESARROLLADOR:
# 1. Crea una copia de este archivo en la raíz del proyecto.
# 2. Renombra la copia a: .env
# 3. Reemplaza los valores de ejemplo de abajo por tus credenciales reales.
# 4. IMPORTANTE: Nunca subas el archivo .env real al repositorio (seguridad).
# =============================================================================

# --- Configuración del Servidor ---
SERVER_PORT=8080

# --- Base de Datos ---
# Ejemplo local: jdbc:postgresql://localhost:5432/nombre_db
# Ejemplo nube: jdbc:postgresql://host:port/db?user=...&password=...
DB_URL=ingresar_url_jdbc_aqui
DB_USERNAME=ingresar_usuario
DB_PASSWORD=ingresar_password

# --- Configuración JWT ---
# Generar una clave Base64 nueva para cada entorno
JWT_SECRET_KEY=ingresar_clave_secreta_aqui
# 1 día (86400000 ms)
JWT_EXPIRATION=86400000
# 2 semanas (1209600000 ms)
JWT_REFRESH_EXPIRATION_MS=1209600000
# false para desarrollo (HTTP), true para producción (HTTPS)
JWT_COOKIE_SECURE=false

# --- Configuración Cloudinary ---
CLOUDINARY_CLOUD_NAME=ingresar_cloud_name
CLOUDINARY_API_KEY=ingresar_api_key
CLOUDINARY_API_SECRET=ingresar_api_secret
CLOUDINARY_FOLDER_NAME=nombre_carpeta
CLOUDINARY_PRESET_NAME=pics

# --- Configuración Frontend (CORS) ---
FRONTEND_URL=http://localhost:4200

# --- Configuraión Google ---
GOOGLE_CLIENT_ID=insertar_google_client_id
GOOGLE_CLIENT_SECRET=insertar_google_client_secret